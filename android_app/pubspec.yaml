name: lmf_app
description: LMF reader and packer
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  image: ^4.5.0
  archive: ^3.6.1
  dynamic_color: ^1.7.0
  file_picker: ^12.0.0
  path_provider: ^2.1.3
  share_plus: ^13.0.0

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
